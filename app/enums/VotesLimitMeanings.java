package enums;

public enum VotesLimitMeanings {
	SELECTIONS, // The limit establishes a number of candidates to select
	TOKENS,  // The limit establishes a number of tokens to distribute among candidates
	RANGE // The limit establishes a range in which the value of the vote must be
}
